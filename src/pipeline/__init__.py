"""Pipeline package - orchestrates the full Adaptive Game Model workflow.

Import the runner lazily (``from src.pipeline.run_all import run_pipeline``)
to avoid double-execution warnings under ``python -m``.
"""
